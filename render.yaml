databases:
  - name: tigermaps-db
    plan: free
    databaseName: tigermaps
    ipAllowList: []

services:
  - type: web
    name: tigermaps
    plan: free
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn classmaps.wsgi"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tigermaps-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.7
